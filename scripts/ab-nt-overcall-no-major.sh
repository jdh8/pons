#!/bin/sh
# Historical all-majors runner. The knob now means unbid majors only, and its
# O1 remeasurement is blocked because the prerequisite direct-1NT reader failed.
echo "O1 stopped: the direct-1NT reader gate failed" >&2
exit 2
