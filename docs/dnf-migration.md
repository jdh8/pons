# DNF migration ledger

Campaign: every authored bidding constraint yields a tight union-of-boxes
(`Dnf` of `Envelope`) reading instead of hulling to ⊤ at `Or` — then flip
`set_dnf_reading` on after measurement. Why readings collapse today and how to
read a call off the bidder instead:
[ai-bidder/sampled-projection.md](ai-bidder/sampled-projection.md). Layer
invariants: [bidding-architecture.md](bidding-architecture.md).

This file is the **ledger only**: what landed, under which knob, with which
verdict. Design lives in the docs above and the code.

## The migration rules

- New projection precision that changes a **knob-off hull** sits behind
  `dnf_reading()` (precedent: `AnyLen::project`). Multi-box values are born
  via `Dnf::disjoin` (knob-gated), never raw `union` — `Dnf::intersect` does
  not consult the knob and would prune knob-off.
- **The wrong-seat trap** (found by the C2 dump diff): a legacy composite
  whose legs read the auction (`support`, `!support`) replays its projection
  under the *reader's* context, so its knob-off reading is
  **context-sensitive** — ⊤ at one point of the auction (the leg contradicts
  its own `len` and the empty product widens out; this, not the `Or` hull
  alone, is the mechanism of the measured "every 2/1 reads 0..=37" bug) and a
  wrong-suit box at another. No static box list reproduces that, so a
  re-authoring must keep the legacy constraint for eval/describe/knob-off
  reading and swap in the boxes knob-on only — the `dnf_upgrade` adapter
  (constraint.rs). The per-rule ratchet cannot catch reader-context effects
  (it projects under the authoring-time context); only the `bba-gen` dump
  diff can. **Run the dump diff for every re-authored gate.**

- **A reading change that moves *endpoints* without moving *mass* will still
  break the net** (found by chop C1, 2026-07-26). `features::push_inference`
  gives each seat only `{min,max} ÷ cap` — 8 length endpoints + 2 `points`
  endpoints — so the evaluator and policy nets carry **no prior over the
  range**. Measured: `hcp(10..=20)` has P(10-12) = 0.264 against P(18-20) =
  0.033 (8×) and `E[hcp | range] = 12.97` against a midpoint of 15.00; on the
  length axis `♥ 5..13` and `♥ 5..8` are the *same claim* (E = 5.36 both, mass
  0.1763 vs 0.1762 — nine-plus suits are 0.04% of the 5+ mass) yet the `max/13`
  column moves **1.00 → 0.62**. So an exact closure can be almost pure
  information-free feature perturbation, landing hardest on the widest boxes.
  This is stronger than "out of distribution": the encoding is **not invariant
  to information-preserving re-representations**, so a retrain fixes the
  instance and not the class. Before measuring any hull-tightening chop,
  ask what it does to *mass*, not to endpoints — `examples/probe-closure-features.rs`
  answers both in ~2 s per 2000 boards, no DD, no training run (C-P row: C1
  displaces the evaluator's length ceilings by **1.9–3.9σ** of their own corpus
  spread while rejecting **0 of 409,708** sampled layouts) — and isolate the net
  with `scripts/closure-isolate-ab.sh`'s `--no-ns-bilans` idiom, which splits
  the delta into what the net sees and what the authored gates see.
  Note the net is not *ignorant* of the prior — the MASS row proved the
  fitted `(μ, σ)` at a hull row already **is** the union-conditional, because
  the labels came from real deals distributed over the union at true mass. It
  learned the prior empirically, per endpoint pattern, with no reason to be
  smooth between patterns; a closure relocates queries in pattern space and the
  learned map does not carry over. Re-parameterizing to conditional moments /
  log-mass is the untested fix, and it must be a **replacement** — keeping the
  endpoint columns keeps the non-invariance. The archived `MassOracle` computes
  both in ~0.1 ms (MASS row). Neither MARG (marginal columns *beside* the
  endpoints → par) nor MASS (serve-time re-conditioning of an endpoint-trained
  net → lost) tested replacement.

## Knob matrix

| Knob | Default | Gates |
| --- | --- | --- |
| `set_dnf_reading` | off | `Dnf::disjoin` (build), `Inferences::admits` (accept), `Dnf::tidy` (hygiene), every ⊤→boxes projection upgrade (`shapes`, `Support::project`, `Balanced::project`, De Morgan complements, `dnf_upgrade`, `top_honors`, `Points`→hcp coupling) |
| `set_gauge_membership` | off | `Envelope::admits` (hence `Dnf::contains`, the overlay, and every sampler) also tests the `hcp` + `support_points` bands — the one knob that can *reject legal hands* if a projection over-claims; E0's eval⟹membership sweep is its soundness gate |
| `set_sum_closure` | off | `Dnf::tidy` narrows every surviving box's lengths to what `Σ len = 13` implies (`Envelope::narrow_to_sum`), between the ghost prune and the containment dedup |
| `set_upgrade_closure` | off | `Dnf::tidy` closes `hcp` against `points` through the shape upgrade (`Envelope::narrow_to_upgrade`, via `constraint::upgrade_ceiling`) |

Every knob has a [bidding-options.md](bidding-options.md) Engine row and a
`bba-gen` arm flag (`--no-ns-dnf`, `--ns-gauge-membership`,
`--ns-sum-closure`, `--ns-upgrade-closure`).

## Chops

Byte-identical chops keep the default system byte-identical (dual-knob
ratchet + `verify::compare`/exhaustive-shape tests + `bba-gen` dump diff).
MEASURED chops follow [measurement.md](measurement.md) and record their
verdict here.

The A→E0 wave landed as **one commit** (this session, 2026-07-23): the chops
were developed against shared files and verified byte-identical as a whole —
dump diff 4000 boards × both vuls + a second 4000-board seed, zero divergent
boards.

| # | What | Status |
| --- | --- | --- |
| pre | `Dnf` machinery + `set_dnf_reading` | cff8919 |
| pre | `Strength` gauges (hcp + support_points axes) | dd90e2c |
| pre | Stage A `Flip` complements | 1e11c49 |
| pre | `authored_calls_read_what_they_gate` leak ratchet | 095ac85 |
| A | this ledger | landed |
| B | ratchet upgrade: shared trie walk, `dutch()`, per-gauge noun columns, dual-knob exact pins, per-box leak predicate | landed |
| C | `impl Constraint for Envelope + Dnf`: strict `Envelope::accepts` eval (all gauges, ceilings), identity projection, noun-compatible `describe` | landed |
| C2 | pilot: 2/1 fit-split boxes via `dnf_upgrade` (legacy eval/describe/knob-off reading kept; exact two-box reading knob-on) | landed |
| D1 | bounded-band complements → 2-box DNF via `disjoin`; De Morgan on `And`/`Or` + `Flip` double-negation `project_complement` (knob-gated); `SupportPoints::project_complement` (new, knob-gated) | landed |
| D1b | exact shape DNFs via `shapes()`: `balanced` (cube {2..=4}⁴ + four 5(332)), `semi_balanced` ({2..=5}⁴ ∪ four {s 6..=7, rest 2..=3}), `notrump_shape` per variant ({M 2..=4, m 2..=cap} ∪ two 5M(332)); each pinned exhaustively over the 560-shape lattice | landed |
| D1c | knob-on box hygiene (`Dnf::tidy`): sum-feasibility prune + containment dedup | landed |
| D2 | `Support::project` forward-boxes partner's suit, knob-gated | landed |
| E0 | book-wide eval ⟹ strict-membership sweep over american()+dutch(), knob-on, forward + band (inference.rs, beside the ratchet) | landed |
| G | the conversion tail, knob-on meter → **0 in every column**: comparative staircases (`longer_suit`/`at_least_as_long`/`equal_length`, exact `∪ₖ {b ≤ k, a ≥ k+gap}` — 15 `described` closures replaced, labels kept, eval pinned exhaustively); transfer reroutes + `splinter_short` via `dnf_upgrade` (boxes sound in both treatment states, authoring-time `union` never `disjoin`); `top_honors` floors its suit length + raw HCP (2/5/9 for 1/2/3 honors); `Points` → `hcp` gauge coupling (a points floor implies an HCP floor slacked by `hcp_ceiling_slack` — without it, tidy's *correct* containment dedup swallows the `hcp` arm of `points(22..) \| hcp(22..)` and loses the knowledge); `Balanced::project_complement` (unbalanced = 4 short + 4 long + 12 two-suiter boxes, exact); sniffer stops counting context claims ("partner's last suit is ♠") and no-op caps ("≤13 ♠") | landed |
| E | **MEASURED** `set_gauge_membership`: gauges get membership teeth. Knob + harness landed (default off, byte-identical); `ab-dnf-sd-lead` runs the **in-process knob matrix** off/dnf/gauge/both on one bid-out (tighter than the planned per-arm `--arm` flag — every arm prices the identical lead question). **Verdict: WASH every cell** (20000 boards × both vuls, seed 1784779888, 16 worlds): gauge vs off −0.0034 ±0.0262 NV / −0.0042 ±0.0309 vul; on top of dnf +0.0140 ±0.0261 / +0.0002 ±0.0317. The expected outcome — no regression, teeth are free; the knob stays **default-off** as the independent kill-switch, its on/off ride-along decided with F. *Side reading, F caution:* the post-G `dnf` arm's NV cell is a significant sd-lead **loss** (−0.0318 ±0.0267) where the pre-G dnf arm measured a wash; vul-both +0.0121 ±0.0315, pooled a wash. If F's match loses, first suspect a G-era box family over-tightening the leader's sampled worlds | landed |
| F | **MEASURED** flip `set_dnf_reading` default-on. Harness: `--ns-dnf` + `--ns-gauge-membership` arm flags in `bba-gen`, `scripts/dnf-flip-ab.sh` (off/dnf/both arms). **Verdict: flip REFUTED as-is — LOSS in all four cells** (204800 bd/arm/vul, SEED_BASE 1784784503, sha bb32624): dnf-vs-off plain −0.0038 ±0.0030 NV / −0.0054 ±0.0036 vul, PD −0.0039 ±0.0036 / −0.0053 ±0.0043; fired 1.02%/0.91%, −0.37/−0.60 per fired. Worst-tail fingerprint = **under-reach** (ON stops in 5♣ / passes out 5♥X where OFF bids the making slam) — consistent with the plan's predicted feature shift: the frozen BBA-distilled net is calibrated to *hulled* features, so truthfully tighter hulls read as "partner is weaker" (hypothesis, not traced board-by-board). `both`-vs-`dnf`: **0 fired in 409600** — gauge membership is bidding-inert, confirmed at scale. Knob stays **default-off opt-in**; the flip's remaining path was thought to be regen+retrain with knob-on features — **refuted by F1** (features are knob-invariant; see the F1 row). Ratchet re-pin and `--no-ns-dnf` rename are flip-day tasks and stay parked | landed (flip refused) |
| F1 | **FORENSIC — F's loss traced to the bilans evaluator reading knob-shifted ranges; the retrain that matters is the *evaluator*, not the policy floor net.** Full-list re-diff of the surviving F artifacts (`ab-dump-diff --show 3000`, verdicts reproduced exactly) splits the delta: **contested first-divergence is net positive** (+177 NV / +162 vul over ~1750/1500 boards — the sampler pinning works as designed) and the **entire loss is constructive** (−957 NV / −1276 vul on ~350 boards), dominated by slam/level conversions deep in 2/1 auctions (pass→6NT over 3NT −250/−312, 6NT→7NT −140/−189, pass→4♣/4♦ raises, and under-raises like 4♠→pass vul — wrong in *both* directions). Board-level trace (probe-classify `--dnf`, board `KQ964.Q2.KQJ8.A4` at `1♠-2♣-2♦-3NT`): the 6NT blast fires knob-on from instinct's NT-slam milestone with `combined_hcp(33)` **false** (18+0) — the rule enters through `points_or_net`'s **net arm**, because `set_bilans_floor` ships **default-on** (ca18fb4). Mechanism: **two hull regimes.** Bare-context hulls (`Context::new`, the `dump-teacher` feature path) are knob-invariant — measured byte-identical over a 21K-row dump (temporary `--dnf` toggle, removed); but **prefixed-context** hulls (`Stance::infer`, what the bidder actually sees) DO tighten knob-on via the authored-projection overlay (the traced board: partner gains ♠≤3, sp≥13 from the C2 fit-split boxes). `trick_estimates` feeds those prefixed ranges to the **evaluator net, which was fit on knob-off prefixed readings** (evaluator.rs's own distribution warning) — knob-on inputs are out-of-distribution and the bilans game/slam gates misfire both ways. Consequences: (1) retraining the **policy** floor net from `dump-teacher` as-is is a no-op (bare-context corpus is knob-invariant); (2) the flip's real coupling is `set_dnf_reading` × `set_bilans_floor` — candidate next chops: **F2a** compat shim (compute `trick_estimates`' ranges under knob-off reading while the sampler keeps the boxes; isolates the contested gain from the evaluator OOD loss) and **F2b** regen+retrain `evaluator_v2` on knob-on prefixed features, then re-flip; (3) **side finding, knob-independent:** the policy net *serves* on prefixed features (`NeuralFloorBba::classify` gets the trie context) but was *trained* on bare-context features — a standing train/serve skew worth its own experiment | landed (docs + probe tooling) |
| F1 | **REFUTED PREMISE — retrain is a no-op.** The F verdict's "regen+retrain with knob-on features" path assumed the flip shifts the net's inputs. Probed 2026-07-23: a `--dnf` build of `dump-teacher` (`--teacher bba --boards 2000 --seed 1`, 21,456 rows) produced a **byte-identical `.f32`** to the knob-off dump — `features_v3` is knob-**invariant**, so retraining consumes identical data and emits the identical net. Structural reason, twofold: (a) `Dnf::disjoin`'s off-path is exactly the on-path's bounding box (hull of a union = union of hulls, componentwise), so born disjunctions never move the hull; (b) the hand-authored `Inferences::read` walk already narrows what the knob-gated projection upgrades (e.g. `Support::project`) would add, so the overlay's knob-on hulls intersect in redundantly. `PartnerShownLen`/`PartnerShownPoints` gates also read the hull (`constraint.rs` eval via `.partner()`), so they are knob-invariant too — the F entry's "hull tightening → net features + PartnerShownLen gates" mechanism is dead. **The flip's entire bidding delta flows through membership** (`Inferences::admits` any-box → the sampler → DD-priced decisions), which re-points the under-reach at the chop-E side-reading suspect: a box family (likely G-era) over-tightening sampled worlds so DD prices slams as failing. Next path: forensic trace of the existing `ab-results/dnf-flip` divergent boards to the flipped decision and its box family — **not** a retrain. The stale claim in `set_dnf_reading`'s doc comment is corrected in the same change | probed, retrain path closed |
| F2b | **IN FLIGHT — knob-matched evaluator retrain, then the re-flip.** Regen + retrain `evaluator_v2` on knob-**on** prefixed readings, exactly the shipped recipe with only the corpora swapped: `dump-evaluator --dnf` (new flag; single-threaded, knob set once; sidecar records `"dnf"`) over 1M deals of `22.pdd` seed 1 + the 20k-deal shard held-out seed 1784555275, `--encoding bits --oracle` (the built trainer is the 87-wide era; the ben mask keeps the same 54 columns), trainer `--arm ben --hidden 256 --epochs 150 --batch 4096 --lr 0.001 --seed 1`. Corpus 20,145,943 rows (knob-off: 20,143,935 — the auctions themselves move knob-on, as they should). **Held-out gate PASSED: val NLL −1.51183 / MAE 1.441 tricks on the knob-on held-out vs the shipped net's −1.51052 / 1.4429 on knob-off** — the OOD penalty is gone at equal fit quality. The 87-era trainer exports at corpus width; the artifact is gathered post-export to the 54 ben columns (exact — dead columns are zero-input), and `dnf_matches_candle_fixture` pins the gathered forward pass against candle's outputs. Serving: `evaluator_v2_dnf.{f32,json,fixture.json}` embedded beside the shipped artifact, **selected per call by `dnf_reading()`** in `forward` — knob-off never touches it, byte-identity structural. **Traced-board sanity: fixed.** `KQ964.Q2.KQJ8.A4` at `1♠-2♣-2♦-3NT` knob-on now tops out at Pass +7.99 with 6NT at −13.1 (was: 6NT fired +1.60). Proof harness: `scripts/dnf-flip2-ab.sh` (off vs dnf only — the gauge arm dropped, 0 fired in 409,600), fresh seed, 204,800 bd/arm/vul, plain+PD decision table. **Round 1 (SEED_BASE 1784808503): every cell improves vs F and every CI now spans 0 — plain −0.0026 ±0.0046 / −0.0032 ±0.0056, PD −0.0037 ±0.0052 / −0.0050 ±0.0063 NV/vul — but still leaning negative.** Classifier verdict: contested first-divergence **wins bigger than ever** (+659 NV / +735 vul), constructive still loses (−1192/−1399), and the loss is ONE family: pass→4NT after partner's 4M signoff — `[- 4♠ -] -->4NT` **−1388 NV / −1904 vul** on 204/227 boards plus `[- 4♥ -] -->4NT` −613/−658 | round 1: wash, one family |
| F2b′ | **The 4NT family traced to the wrong-suit trap in Jacoby 2NT's projection, and fixed.** Board trace (`AQJ85.AKT743.3.9` at `P-1♠-P-2NT-P-4♥-P-4♠-P`): knob-on partner reads ♠4..13 (the Jacoby support, correctly) → the floor's M6.4 RKCB ask fires 4NT (+1.68) — *rightly*, opener holds all five keycards + ♠Q and 6♠ is cold. Partner answers 5♦ (3 keycards, correct 1430). **Then the asker passes 5♦**: at the answer node the knob-on reading shows partner **♦4..13 with ♠ back to 0..13** — the `support(4..)` leg of Jacoby 2NT projects under the *reader's current context* and re-targets to partner's last-shown suit; the off-book floor 5♦ (alerted `RKCB_FLOOR` but invisible to the reader — the instinct floor is not in the trie, so `authoring_classifier` can't decode it) reads as natural diamonds, moving the whole Jacoby box to ♦. `keycard_trump` then can't count 8 spades → `keycard_answered` = None → all completion rules −∞ → settle floor passes the answer. −13 IMPs a board, ~430 boards/run. **Fix (the C2 idiom): `jacoby_box(major)` — Jacoby 2NT re-authored via `dnf_upgrade` with the box pinned statically to the node's own major** ({major 4..13, sp 13..37}; legacy keeps eval/describe/knob-off reading; eval-equivalence pinned by `jacoby_dnf_matches_composite`). Traced board now completes **6♠ +1.84**. Sibling latent instances (limit raise etc.) left for measurement to demand. Residual small families parked: pass→4m raises over 3m (−156/−96 NV), 3NT→6NT (−64). **Round-2 A/B (SEED_BASE 1784809754, 204800 bd/arm/vul): the flip WINS all four cells, CIs clear of zero — plain +0.0094 ±0.0046 NV / +0.0080 ±0.0056 vul, PD +0.0118 ±0.0051 / +0.0085 ±0.0062; +0.45..0.65 IMPs per fired board (~1.8%).** Classifier: both phases positive (NV plain: constructive +1158, contested +766); the 4NT-strand family is gone (the 4♥ variant nets −56 over 253 boards — keycard variance, not a strand). Residual tails are the parked 4m-raise family (−160/−98) and a 29-board 4♠→6♠ over-reach (−143). Per docs/measurement.md this is **shippable default-on** (plain win + PD win, both vuls). Flip-day items now actionable: default `set_dnf_reading` on, `--ns-dnf`→`--no-ns-dnf` rename, ratchet re-pin, gauge-membership stays off (inert) | **WIN 4/4 — SHIPPED** |
| FLIP | **`set_dnf_reading` default ON (flip day, 2026-07-23).** Knob default flipped; `bba-gen --ns-dnf` → `--no-ns-dnf` (the legacy-hull off-switch); `probe-classify --dnf` → `--no-dnf`; `dnf-flip2-ab.sh` arms re-expressed off the new default. Re-pins, all deliberate: the `artificial_calls_are_alerted` family and the axis-leak/projection pin tests now set their knob state explicitly (the structural `artificial` detector is defined against hulls — knob-on fit-split boxes false-positive it, so `unalerted_artificial` pins knob-off internally); two marginal-slam behaviour pins moved with the evaluator (`fit_sum_reads_a_four_four_major_fit` / `bilans_floor_still_bids_the_known_fit_game`: the 66.3%-DD 6♠ prices back to 4♠ under the knob-on read + twin — both regimes now pinned in-test), and `competitive_rebid_reaches_the_missed_game`'s 5♦ raise reads 4♦ knob-on (both pinned). **PARKED — splinter shortness cap lost knob-on:** at `1♠-P-4♣-P` the knob-off reading caps partner's ♣ ≤1; knob-on the projection overlay yields a single ⊤-length box (the cap lives in the hand-written reader / an opaque shortness leg the overlay can't project), losing the cap from hull AND sampler. Inside the measured-winning package, but a real reading hole — next chop candidate: author the splinter box (`dnf_upgrade`, the Jacoby idiom). `probe-keycard-reach`'s realization test pins knob-off meanwhile | shipped |
| G0 | **MEASURED** 2NT shape redesign {M 2..=4, m 2..=6} (drops 5M(332), adds wide minors) — a treatment, not a rewrite. Knob `set_two_notrump_wide` (default off, byte-identical) + reading widen (opener's minors 2–6 under the knob); harness `--ns-two-nt-wide` in `bba-gen` + `scripts/two-nt-wide-ab.sh` (off/wide arms, plain+PD). sd-lead parked (disclosure mismatch — needs an in-process knob matrix). **Verdict: micro-positive WASH, SETTLED — not shippable, not refuted.** First run (SEED_BASE 1784788055, 204800 bd/arm/vul) leaned **+0.0009/bd** in all four cells but every CI spanned 0. The 5× re-measure (SEED_BASE 1784789763, **1024000 bd/arm/vul**) regressed the lean by half: plain none **+0.0004 ±0.0005**, pd none +0.0005 ±0.0005, plain both +0.0004 ±0.0006, pd both +0.0004 ±0.0007 — 3/4 cells still span 0 and plain-DD primary never clears → **WASH**. Effect is *real but negligible*: all four cells stay positive & same-order (not pure noise), yet per-fired regressed **+0.5 → +0.28 IMPs** (4/4 same sign) and it fires only **0.15%**. Worst tails repeat identically across all four cells (−14…−21 IMP slam/game misroutes: `A93.AKQT9.KJ.QJ9`, `KQT.Q9.AKT.QJT94`, `AKQ98.AQ.KT8.Q62`, `AK953.853.AK.AQT`); the many small minor-fit wins just outweigh them. **Stop here** — 3σ would need ~9M bd/cell at a magnitude beneath shipping relevance; not a re-measure candidate anymore. Knob stays default-off opt-in. Byte-identity holds (reading widen is knob-gated) | harness landed, opt-in |
| G-tail | term-merge if `debug_assert!(< 64)` ever fires; `described` closures whose labels name no axis (e.g. "spades longer than hearts, or equal five-plus" in the 1M responses) are invisible to the meter and stay ⊤ — convert on demand | parked |
| MARG | **MEASURED (NLL gate) — feature-eval + marginal nets: hidden seats as card marginals through a marginal net + inclusion–exclusion; the flip was NOT taken, everything ships opt-in.** The campaign (plan: the marginal-eval design sessions, 2026-07-24) aimed the evaluator at pure cards-language inputs — independent of bidding system AND evaluation gauges, with future reading chops needing only a CPU-cheap marginal refresh. **Probe** (`probe-marginal-boxes`, 20k deals × 2 systems): ~96% of prefixed readings are single-box; multi-box 3.6/4.3% (american/dutch) and their boxes essentially ALWAYS overlap — the 14–16-box shape complements form ONE overlap component, so exact I–E is unpayable there; cap set at `IE_CAP = 5` (exact subset expansion; larger components truncate to singles + signed pairs, ≤ c+c(c−1)/2 queries). **Marginal net**: `src/bidding/marginal.rs` — `monte_carlo_marginals` (pin own hand, deal 39, condition ONE seat on lenient `admits`; exchangeability makes one net serve all seats), `ie_plan`/`compose` (signed query plan, f64 accumulation, cancellation guard, clamp; plan shared verbatim by dump and serve), `features_marginal` (24 own-hand + 14 box floats). Corpus `dump-marginal`: 1,035,013 rows / 20k deals / **8.5 min wall, no DD, no solver**; labels ≤0.01 SE at the 2k-accept budget. Trainer `trainer/src/bin/marginal.rs` (BCE on soft targets for honors+mass = proper scoring rule, MSE spots, accept-weighted): **h128 held-out honor-MAE 0.0166 ≈ 2× the MC noise floor, mass reliability diagonal in every bin** — shipped as `marginal_v1` (~25k floats, candle-parity fixture + `weights_fingerprint` train/serve tripwire). **Evaluator v3** (`dump-evaluator --encoding marginal --dnf`, 20.18M rows over `22.pdd` seed 1, 62-min dump; same trainer recipe, `--hidden 256`): held-out **NLL −1.4983 vs the shipped twin's −1.5118, MAE 1.456 vs 1.441, slam-MAE 2.677 vs 2.646 — the marginals-only flip LOSES the NLL gate**. **Hybrid diagnostic** (`--encoding hybrid`, 54 exact hulls + 72 marginal cols): **NLL −1.51178 ≈ par (−1.51183), MAE identical 1.441**, slam-MAE 2.635 vs 2.646 — marginals recover their own deficit and add ~nothing beyond the hulls. Conclusion: **the exact hull bounds are already the sufficient statistic for DD tricks at current reading fidelity** — estimated honor marginals (0.0166/col net + per-seat independence) can't beat exact facts, consistent with the BEN-oracle finding that even TRUE hidden-honor columns bought little. The A/B was never spent: the NLL gate refuses the flip before it, per the campaign's checkpoint design. **Code DROPPED by decision (2026-07-24) — this row is the record.** Nothing landed in the crate: no knob, no twin, no module; the default system was never touched. The uncommitted implementation (module, dumper, probe, trainer bin, A/B script, both weight sets, wiring diff) is archived outside git in `target/marginal-campaign-archive/`, corpora + trained nets in `target/` (`marginal-corpus`, `eval-{train-1m,test}-{marginal,hybrid}`, `marginal-h{64,128}`, `evaluator-hybrid`), design + checkpoint log in `~/.claude/plans/golden-wiggling-squid.md`. Re-opening triggers: a marginal-net fidelity jump (joint cross-seat conditioning, more axes), a reading-fidelity jump (splinter box et al.), or output-side uses of the calibrated mass head (box-mass metering, tidy pricing) — the whole pipeline reran in ~2 h wall, so reconstruction is cheap | measured, DROPPED (record only) |
| MASS | **MEASURED (NLL gate) — exact hypergeometric mass oracle built and validated; serve-time exact-union conditioning of the evaluator REFUTED; code DROPPED (this row is the record).** The oracle (`MassOracle`) priced `P(hidden hand ∈ reading \| seen hand)` in closed form — per-suit 2^k subset census (real `eval::hcp` + `blocks_upgrade`, zero semantic drift) → shape-walk convolution into (shape, HCP, veto) fibres; a union costs one any-box sweep, killing MC **and** the MARG row's I–E cap machinery for masses. Pinned to lenient `admits` on every `PointScale`; `probe-exact-mass` (2000 deals × 2 systems): MAE vs 50k-draw MC = the binomial noise floor exactly, build ≈0.35 ms, union query ≈0.1 ms (vs ~15 ms/MC estimate); **hull admits ~2× the true union mass at the median on multi-box readings** (relative leak p50 43–49%, p90 75–78%). That leak motivated the kill-test: `probe-union-eval` (20k `22.pdd` deals × 2 systems, trainer-formula NLL, ~22k/25k multi-box nodes) scored each multi-box node three ways through the shipped `evaluator_v2_dnf` — production hull row; *tight* (block = dnf hull; **Δ ≈ +0.0005, a no-op — the served envelope IS the dnf hull** except 42/~66k seat-readings, a minor curiosity: the served points floor ran looser than the boxes' hull there); *mixture* (union disjointified by prefix set differences `w_i = m(∪_{≤i}) − m(∪_{<i})` — exact masses, no signed I–E, no cancellation — one net query per box, densities mixed). **Verdict: the mixture LOSES — Δ +0.069 ±0.002 (american) / +0.095 ±0.002 (dutch) nats, MAE 1.34→1.40/1.34→1.44 tricks, mix better on only ~36% of rows.** Mechanism from the top-box arm (each multi seat at its most-massive box alone): **+0.18/+0.21, exploding to +0.43 at k≥6** — narrow-box queries are OOD for a net fit on hull rows, and the mixture only averages that error down, never out. The deeper reason: at a recurring multi-box node the training labels came from real deals distributed over the union with the true masses, so **the hull row's fitted `(μ, σ)` already IS the union-conditional** — serve-time re-conditioning adds extrapolation error and no information. Third confirmation of hulls-suffice (MARG, BEN-oracle Phase 3). No A/B was owed — NLL-gate refusal kills the flip pre-A/B per the MARG precedent. **Nothing landed in the crate**; the implementation (mass.rs module + 6 pins, both probes, `trick_estimates_for` diagnostic entry, wiring) is archived outside git in `target/mass-oracle-archive/` (full commit patch + raw files; reconstruction ≈ 1 h wall). Note the oracle itself did not fail — it is exact and ~150× faster than MC; it died with its only consumer. Re-open on: an output-side consumer that bypasses the net (sampler mass budgeting, tidy pricing, reading forensics), or a *training-side* per-box-conditioned corpus after a reading-fidelity jump makes box identity informative | measured, DROPPED (record only) |

| SHCP | **Per-suit HCP axis** (`Strength.suit_hcp: [Range; 4]`, cap 10 = AKQJ), byte-identical by the dd90e2c idiom: lenient `admits` never reads it (`gauge_membership` off), no default-on consumer, `features.rs` untouched; `subset_of` gains the axis so tidy dedups are only *lost* (a lost dedup keeps an old-axis-⊆ box — membership- and hull-inert); dump **byte-identical** vs 090903a, all 128 shards, 4000 bd × both vuls × 2 seeds (1784963180 / 1784971099). `SuitHcp` gains all three folds, **exact both bounds** (own-scale — the first *forward* projection carrying a ceiling, intended); `top_honors` floors the suit's own HCP (2/5/9) beside its whole-hand floor. **Canonicalize couplings: none, by design** — every candidate (`hcp.min ≥ Σ suit mins`, length-capped suit ceiling, …) either writes an old axis (a shipped-reading change) or manufactures the containment that lets tidy's correct dedup swallow the arm carrying the suit knowledge (the G `hcp_ceiling_slack` lesson, in reverse); the len↔suit-HCP cap tables ship as exhaustively pinned *test-side contracts* (`suit_hcp_cap_tables`) for future gauge-membership/stopper work. New `suit HCP` ratchet column pins **0/0** — knob-on 0 because the folds are exact; knob-off 0 because every walked `suit_hcp` gate (Ogust, Lebensohl trap pass) is `&`-chained and the ungated base-axis write survives the hull. **Meter blind spot found (pre-existing, every column):** the `Or`-shaped gates (UVU double, penalty X, SOS runouts) are wired as `Fallback::classify` and `for_each_authored_rule` walks trie-node classifiers only — the fallback-competitive layer has never been metered or E0-swept; widening the walk moves every knob-off pin, its own chop. Chop-E staleness noted on the knob doc: gauge membership's 0-fired verdict predates this axis (Ogust ceilings have real teeth). Consumers (Ogust quality read, penalty leave-ins, `stopper_in` staircase) are follow-up measured chops | landed |
| C1 | **MEASURED (in flight) — close each box over `Σ len = 13`.** `Envelope::sum_feasible` (D1c) only ever *tested* the 13-card sum to drop ghost boxes; nothing narrowed with it, so a both-majors reading stored `{♠ 5..13, ♥ 5..13, ♦ 0..13, ♣ 0..13}` when eight of the thirteen cards are already spoken for. `Envelope::narrow_to_sum` is one sweep of `len_i.max ← min(hi_i, 13 − Σ_{j≠i} lo_j)` and its dual — bounds consistency for a single linear equality, achieved in one pass, idempotent, and **exact**: the cap is attained by giving `i` that value and distributing `13 − len_i` over the others, feasible for any `sum_feasible` box. Runs in `Dnf::tidy` between the ghost prune and the containment dedup. **Membership-inert** (every real hand satisfies the sum), so `admits`/`contains` are unchanged and the sampler cannot move — the deltas are `Dnf::hull` (tighter, feeding the evaluator + feature nets) and `subset_of` (more real containments, fewer terms). Exactness *is* what makes the dedup safe here, unlike the SHCP couplings: the narrowing never widens an arm's apparent claim. Pins: `sum_closure_is_exact_and_inert` (brute force over all 560 shapes — bounds attained, membership preserved, idempotent) and `closure_is_membership_inert` (proptest, real deals × five authored readings, lenient `contains` **and** strict `accepts`). Knob `set_sum_closure` default off + `bba-gen --ns-sum-closure`; harness `scripts/closure-ab.sh` (off/sum arms, plain+PD, both vuls, 204,800 bd/arm/vul). Smoke: **8.0% of boards diverge** (3000 bd, seed 1785010793). **Verdict: REFUTED — LOSS in all four cells, by an order of magnitude** (204,800 bd/arm/vul, SEED_BASE 1785011107, sha b45a176): plain **−0.0370 ±0.0059** NV / **−0.0463 ±0.0071** vul, PD **−0.0667 ±0.0069** / **−0.0750 ±0.0081**; fired 3.57%, −1.04/−2.32 IMPs per fired. PD loses *worse* than plain in both vuls — the ON arm is getting doubled, not merely misjudging. Worst tails share a fingerprint: **Unusual-2NT / two-suiter auctions** (`1♦ 2NT X XX`, `1♦ 2NT 3♦ 4♦ X`, `- 1♦ 2NT 3♦ 4♥ 5♦`), which is exactly C1's biggest bite — `and([♣,♦], 5..)` narrows `{♣5..13, ♦5..13}` to `{♣5..8, ♦5..8, ♥0..3, ♠0..3}`, capping the overcaller's *majors* for the first time. The reading is **true**; the pins rule the sampler out (membership-inert), so the loss flows entirely through hull consumers. Two candidates, separated by the `closure-isolate-ab.sh` pair (`--no-ns-bilans` on BOTH arms, new flag): **(a)** the bilans evaluator net reading hulls it was never fit on — the chop-F1 mechanism, at ~3.5× F's firing rate; **(b)** authored gates reading the new bounds (`fit_sum_game`, `keycard_trump`, `support_floor` all read `.min`, and the closure *raises* mins — `{♠0..3, ♥0..3, ♦0..3}` now implies `♣ ≥ 4` — against thresholds tuned without it). **Isolation run** (`scripts/closure-isolate-ab.sh`, `--no-ns-bilans` on BOTH arms, SEED_BASE 1785011754, same size): plain **−0.0133 ±0.0050** NV / **−0.0191 ±0.0059** vul, PD **−0.0332 ±0.0060** / **−0.0394 ±0.0068**, fired 2.97%/2.57%. **The net is 47-64% of the loss — the plurality, never a majority on PD — and C1 still loses decisively without it.** Net share by cell: plain 64%/59%, PD 50%/47% (NV/vul); PD amplifies the authored-gate half, matching the doubled tails. So **both** mechanisms are real: (a) the net, explained by the endpoint-encoding rule above — C1's flagship narrowing moves `max/13` by 0.38 while moving the information by 0.06%; and (b) authored gates, whose tails are redoubles and under-reaches (`(1♦) 2NT (X) → XX` instead of the cold 4♥ over partner's 5-5 Unusual; `3♣X` passed out where OFF bids a cold 6♦). Consequence for the re-open path: the F2b twin retrain alone is **not sufficient** — it would recover at most ~60% of a loss that is 5-8× the size of anything the twin has previously fixed. Knob stays **default-off opt-in**. Honest read: C1 is a *true* reading its consumers are not built to receive | **REFUTED 4/4, opt-in** |
| C2 | **MEASURED — close `hcp` against `points` through the shape upgrade. Verdict: BIDDING-INERT (0/3000 boards), and the *reason* is the finding.** `points = hcp + upgrade`, and `upgrade` (`!balanced + (longest_two ≥ 10) − nwasted`) is a function of shape and honor placement, so the box's own `lengths` bound it. Every balanced shape holds ≤9 cards in its two longest suits, hence **balanced ⇒ upgrade ≡ 0**: a box whose lengths force balanced reads `points == hcp`, where `Points::project` slacks `hcp` by the *global* worst-case `hcp_ceiling_slack()` (2 on the shipped PointCount scale) — a 2-HCP leak at each end of the most-read strength gate in the book. New `constraint::upgrade_ceiling` is the box-local dual of `hcp_ceiling_slack` (`None` on rule-of-N+8, whose count can fall *below* raw HCP so the `points ≥ hcp` leg fails); `forces_balanced` brute-forces the box's own compositions with an early exit. Landed, pinned (`upgrade_closure_crisps_the_balanced_band`: `balanced() & points(15..)` reads hcp 13..37 knob-off, **15..37** knob-on), and **it changes no board**. Why: **`Envelope::strength.hcp` is a write-only axis.** Nothing consumes it at default settings — lenient `admits` tests lengths + `points` only (`gauge_membership` off), `features::push_inference` hands the policy and evaluator nets 4 length pairs + the `points` band and nothing else, and no gate reads the band (only `describe_box` and the strict `accepts`). The closure's `points`-side legs are live but idle too: forward `Points::project`/`Hcp::project` write **floors only**, so `points.max ← min(points.max, hcp.max + ceiling)` has no ceiling to bite; the two-sided `hcp_band` (15..17 → points 15..19 → **15..17** on a balanced box) reaches the hull only through the Pass reading. Knob `set_upgrade_closure` kept default-off as the **enabler**, not a win: it is what makes a `set_gauge_membership` flip worth re-measuring (chop E's 0-fired verdict predates both `suit_hcp` and this). Re-open trigger: any consumer of the `hcp` axis — gauge membership, an `hcp`-aware feature column, or a two-sided forward projection. **CORRECTION (probe C-P, 2026-07-26): C2 is *not* membership-inert**, and the landing doc said it was. It bounds `points`, which `admits` tests, using `hcp`, which `admits` ignores — so it hands the unenforced HCP claim teeth through `points`, and the sampler *does* move: a box reading `hcp ..=8` with `points` slacked to `..=10` narrows back to `..=8` once its lengths force balanced, and the 9- and 10-counts the sampler was dealing outside the stated band stop being dealt. 249 rejections in 8,576 cross-tested layouts (vs C1's 0 in 409,708); pinned by `upgrade_closure_gives_hcp_teeth`. Bidding-inertness (0/3000 boards) still holds — it is simply rarer than a call-flip needs to be. Consequence: a `set_gauge_membership` re-measure with C2 on is **two** reading changes, not one | landed, bidding-inert, opt-in |
| C-P | **PROBE (no training run) — `examples/probe-closure-features.rs`: does a closure move the net's inputs without moving the distribution those inputs describe?** Bids a corpus under the baseline (so the arms cannot diverge in *which* readings they see), and at every auction node reads it under both knob settings, diffing the 30 hidden-seat values of `features_eval` — the evaluator's actual input — and cross-testing `sample_layouts` draws against the other arm's reading. **C1 (2000 bd, 20,669 nodes, seed 1785099001): endpoints move at 15.7% of nodes, moments at none.** Only the four length *ceilings* move (the floor leg needs `13 − Σ others' max` to bind, which unknown suits never let happen): mean |Δ| **0.36 feature units ≈ 4.6 cards**, p90 0.46 (6 cards), max 0.77 (10 cards) — **1.9σ (majors) to 3.9σ (minors) of each column's own corpus spread**, i.e. the net is handed an input displacement several times its typical variation. Membership: **0 rejections in 409,708 layouts**, both directions. That is the endpoint-encoding rule measured rather than argued: C1's perturbation is information-free by construction, so the ~60% net share of its loss is pure encoding non-invariance, and no retrain can be *earned* on it. **C2: 0.32% of nodes, `pts max` only, 0.09σ — but 249/8,576 membership rejections**, the correction recorded in the C2 row. Cost: ~2 s per 2000 boards, no DD, no solver. **`--gauge` (gauge membership on in *both* arms) closes the owed C2 × gauge re-measure without a run: C2's 249 rejections drop to 0/8,576, C1 stays 0/405,546.** Subsumption, and it is a theorem: C2 bounds `points` by `hcp + upgrade_ceiling(lengths)`, every hand inside the box has `upgrade ≤ upgrade_ceiling`, so any hand the closure rejects the direct `hcp` test rejected already. So stacking C2 on gauge membership adds *only* the 0.09σ `pts max` endpoint move — the artifact-shaped direction C1 was just refuted for — on top of a knob already measured WASH. **Re-measure: NO-GO.** Side note explaining that WASH's nonzero sd-lead delta despite 0/409,600 bidding divergence: `single_dummy.rs:364` samples its defender worlds from the same `Inferences`, so gauge membership moves the *scorer*, not the system | landed |

## Ratchet counts

`authored_calls_read_what_they_gate` pins leak counts (a leak = an authored
rule whose describe names an axis while **no box** of its band constrains
it). Knob-off pins are the byte-identity guard (exact — must not move in
either direction); knob-on pins are the migration meter later chops drive
toward 0. Cells are knob-off/knob-on; knob-off never moved during the wave.

| After chop | HCP | length | points | support | support points |
| --- | --- | --- | --- | --- | --- |
| 095ac85 (old columns) | — | ≤49 | ≤61 (HCP+points mixed) | — | — |
| B | 17/0 | 71/29 | 3/0 | 84/84 | 18/0 |
| D1 | 17/0 | 71/24 | 3/0 | 84/84 | 18/0 |
| D1c | 17/7 | 71/47 | 3/3 | 84/84 | 18/12 |
| D2 (wave tip) | 17/7 | 71/47 | 3/3 | 84/0 | 18/12 |
| G | 17/**0** | 59/**0** | 3/**0** | 84/**0** | 18/**0** |
| Points13 (2/1 gate, 2026-07-25) | **11**/0 | 59/0 | **9**/0 | 84/0 | 18/0 |

The `Points13` row is **not a chop** — it is the shipped default flip of the
major no-fit 2/1 gate (`hcp(13..)` → `points(13..)`, docs/bidding-options.md).
Six 2/1 rules move the axis their legacy-`Or` reading leaks on: knob-off HCP
17 → 11 and points 3 → 9, a clean swap (the union of `points(13..)` with the
fit leg's `support_points(13..)` bounds points to ⊤, exactly as it bounded HCP
to ⊤ before). Knob-on stays **0/0** on both — the `dnf_upgrade` fit-split box
pins `points(13..)` on the no-fit box exactly, so the shipped DNF-on reading
never leaked. Re-pinned in the same commit per the ratchet's re-pin rule.

The SHCP chop adds a sixth pinned column, `suit HCP`, born **0/0** ("HCP in
♠" atoms against the per-suit HCP axis; the length column's "… in ♠"
exclusion stands). Knob-off 0 is genuine but scoped: see the SHCP row's
fallback blind spot.

G's knob-off `length` 71 → 59 is **sniffer precision, not a reading change**
(dump diff clean): the twelve dropped entries are the eight "partner's last
suit is x" context claims and the four "≤13 x" deliberate no-op caps, which
never claimed anything about the hand.  The knob-on meter is **done** — every
authored rule that names an axis now reads a box constraining that axis; the
exact pins guard the zero.

D1c's knob-on **rise** is the meter getting honest, not a regression: an `Or`
with an opaque arm used to read `[tight-box, ⊤]`, which is membership-wise
just ⊤ — containment dedup collapses it to `[⊤]`, so phantom axis knowledge
no longer counts. The knob-on residue now points at real work: the opaque
`described`/`pred` arms (G) still co-gating rules that name an axis.

To list the rules behind any cell, zero that pin and run the test — the
failure dumps the per-column rule labels. The knob-on lists are chop G's
worklist.

## Irreducible tail (stays ⊤, by design)

`suit_hcp` left the tail with the SHCP chop — it has its own axis and exact
folds. What remains, each with its re-open trigger:

- **Stoppers.** `stopper_in(fixed suit)` is context-free and its exact reading
  is a 4-box staircase ({sh≥4} ∪ {sh≥3, len≥2} ∪ {sh≥2, len≥3} ∪ {sh≥1,
  len≥4}), but three arms floor *lengths* — a shipped-reading tightening, so
  it is a **measured** follow-up, not a byte-identical one.
  `stopper_in_their_suits` resolves the reader's context — the wrong-seat
  trap; only `dnf_upgrade` with statically pinned suits could box it.
- **Keycards.** RKCB answers read as opaque closures (`slam.rs`); keycard
  info is orthogonal to every current axis, so membership teeth could bite.
  Re-open trigger: the owed `probe-keycard-reach` evidence (the F2b′ −13
  IMP/board family was trump *identity*, already fixed via `jacoby_box`).
- **Controls.** No predicate exists in the rule language — author one when a
  convention needs it, then consider the axis.
- **Alt scales** (`fifths`, `cccc`, `nltc`): no axis, no named leak.

Context atoms (`min_level_is`, `they_bid`, seats, vulnerability) are
hand-vacuous — ⊤ is *exact*, not a leak.  And the **fallback blind spot**
(SHCP row): rules wired as `Fallback::classify` are invisible to the meter
and the E0 sweep on *every* column — a meter-scope chop of its own if the
fallback-competitive layer is ever to be metered.  **Now priced** by the ⊤
census (`examples/probe-reading-census.rs`,
[ai-bidder/sampled-projection.md](ai-bidder/sampled-projection.md)): it is not
merely unmetered, it is unprojected — `project_authored` skips any classifier
whose `as_rules()` is `None`, so a pass over a weak two reads 5/5 ⊤ on **100%**
of readings and a pass over 1NT on 90.7%.  Every fully-blind key in the census
is one of these.

The ⊤s that are **real** leaks rather than exact readings all sit at the floor's
game/slam milestones, and there are two kinds, not one:

- **The evaluator net's own gate.** `net_break_even_gate` is a `pred`, so it
  takes the default `project` (⊤), and `points_or_net` disjoins it — knob-on, all
  eleven converted milestones read as nothing.  Not a missing fold: a `pred`
  accepts hands no box contains, so ⊤ *is* the sound projection.
- **The pair-level point gates themselves** — `combined_points`, `combined_hcp`,
  `fit_sum_game`, `slam_entry_reached`.  All four are `pred`, and no milestone
  call site conjoins an own-hand point band, so `box(authored)` is ⊤ *with the
  net off too*.  The arithmetic no more advertises itself than the net does; it
  is simply the older leak.  A contextual `project` is not the fix — it would
  recurse (`project` → `Inferences::read` → `project_authored` → `project`) and
  re-target to the reader's seat (the wrong-seat trap, F2b′ row).  No static
  own-hand floor is sound either: partner's shown minimum ranges 0…22, so
  `combined 25` implies only own ≥ 3.  Closing it needs
  `Constraint::project` widened to carry the actor's seat and the fold's partial
  readings, or the behavioural reading of
  [`ai-bidder/sampled-projection.md`](ai-bidder/sampled-projection.md).

Both were invisible to the meter for the same reason as the rest of the fallback
blind spot above: the floor is wired as `Fallback::classify`.  See
[`ai-bidder/evaluator-net.md`](ai-bidder/evaluator-net.md) — "The reach
ceiling", and the 2026-07-26 correction under it — for why they are survivable
where they sit (every converted site is a mostly-terminal milestone) and what
each fix costs.
